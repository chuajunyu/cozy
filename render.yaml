# One instance: Cozy's session state currently lives in process memory.
services:
  - type: web
    name: cozy
    runtime: docker
    branch: codex/render-deploy
    plan: free
    region: singapore
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /health
    envVars:
      - key: OPENAI_API_KEY
        sync: false
